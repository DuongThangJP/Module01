let width = Number(prompt('Nhập vào chiều dài: '));
let height = Number(prompt('Nhập vào chiều rộng: '));
let area = width * height;
console.log('Diện tích là: ' + width * height);
alert('Diện tích hình chữ nhật là: ' + area);